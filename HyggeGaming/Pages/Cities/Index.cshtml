@page
@model HyggeGaming.Pages.Cities.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City[0].City1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.City) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.City1)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ZipCode">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ZipCode">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ZipCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
