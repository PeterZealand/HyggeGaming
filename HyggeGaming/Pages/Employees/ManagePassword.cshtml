@page
@model HyggeGaming.Pages.Employees.ManagePasswordModel
@{
}

@* 
<table class="table">
    <thead>
        <tr>
            <th>Team Id</th>
     
        </tr>
    </thead>
    <tbody>
        @if (Model.DevTeam != null)
        {
            @foreach (var team in Model.DevTeam)
            {
                <tr>
                    <td>@team.DevTeamId</td>
                    <td>@team.DevTname</td>
                    
                    <td>
                        <ul class="list-unstyled">
                            @foreach (var member in team.Employees)
                            {
                                <li>
                                    <strong>@member.FirstName @member.LastName</strong>
                                    | Role: @member.RoleId | Email: <a href="mailto:@member.Mail">@member.Mail</a>
                                </li>
                            }
                        </ul>
                    </td>
                    <td>
                        <a asp-page="/Employees/UpdatePassword" asp-route-id="@team.EmployeeId">Update</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

 *@